# A3.1 — workspace.json: схема + merge runtime→project

**Контекст:** Разделение runtime-only и project state.

## Цель
Сделать задачу завершённой и проверяемой по чеклисту ниже.

## Входы
- VTube MVP: архитектурный холст (MVP)
- VTube MVP roadmap

## Выходы (артефакты)
- Workspace schema
- Loader/Saver
- Merge logic

## Шаги
1) Описать схему workspace.json
2) Загружать при старте, восстанавливать состояние
3) Save/Save As делает merge runtime overlay в project state

## Критерии приёмки
- workspace восстанавливается при запуске
- Save фиксирует «текущую реальность»
- runtime-only не сохраняется без Save

## Зависимости
См. зависимости в roadmap (если блокирует — пометь в PR/issue).

## Риски/заметки
Если что-то упирается в дизайн/выбор подхода — зафиксируй решение в docs/ и продолжай, не устраивай архитектурный «битый триксель» 😅
