# A1_8 — Shared контракт: DTO и версионирование Unity↔App

**Status:** todo


## Цель

Создать минимальный набор общих DTO и версионирование контрактов, чтобы Unity и app не расходились по форматам данных.


## Контекст / ссылки

- `docs/unity-app-boundary.md` (SceneCommand, Frame Header)
- `shared/` пустой


## Объём

**Включено:**

- Определить, что должно лежать в `shared/` (минимум: SceneCommand, FrameHeader, ошибки/ack).
- Определить правила версионирования (например, `contractVersion`).
- Документация структуры `shared/`.

**Не включено:**

- Реализация сериализации и использования в Unity/app.


## Шаги реализации

1. Список минимальных DTO для Unity↔App.
2. Решение по версионированию и совместимости.
3. Документация (файл в `docs/` или обновление `docs/unity-app-boundary.md`).


## Критерии приёмки

- [ ] Есть список DTO для shared-слоя.
- [ ] Есть правила версионирования и совместимости.
- [ ] Документация описывает назначение `shared/`.


## Как проверить

- Документы дают однозначные правила, какие типы разделяются между Unity и app.


## Артефакты

- Документация в `docs/` и описание структуры `shared/`.
