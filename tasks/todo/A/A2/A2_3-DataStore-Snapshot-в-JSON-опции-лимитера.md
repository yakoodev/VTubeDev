# A2.3 — DataStore: Snapshot() в JSON + опции лимитера

**Контекст:** Снимок состояния для overlay/стрима.

## Цель
Сделать задачу завершённой и проверяемой по чеклисту ниже.

## Входы
- VTube MVP: архитектурный холст (MVP)
- VTube MVP roadmap

## Выходы (артефакты)
- Snapshot serializer
- Limiter options
- Stress test

## Шаги
1) Реализовать Snapshot() -> JSON
2) Проверка на больших объёмах (>=10k vars)
3) Заложить параметры лимитера объёма (по умолчанию без лимита)

## Критерии приёмки
- Snapshot валидный JSON
- 10k записей не валят систему
- Есть настройки лимитера

## Зависимости
См. зависимости в roadmap (если блокирует — пометь в PR/issue).

## Риски/заметки
Если что-то упирается в дизайн/выбор подхода — зафиксируй решение в docs/ и продолжай, не устраивай архитектурный «битый триксель» 😅
