# B3.3 — bindings.json: параметры → Transform/Material

**Контекст:** Binding engine применяет параметр к объекту.

## Цель
Сделать задачу завершённой и проверяемой по чеклисту ниже.

## Входы
- VTube MVP: архитектурный холст (MVP)
- VTube MVP roadmap

## Выходы (артефакты)
- BindingEngine
- bindings.json schema
- Demo mapping

## Шаги
1) Schema bindings.json
2) Target types: Transform + Material property
3) scale/offset/clamp + демо

## Критерии приёмки
- Два типа таргетов работают
- scale/offset/clamp применяется
- Демо видно

## Зависимости
См. зависимости в roadmap (если блокирует — пометь в PR/issue).

## Риски/заметки
Если что-то упирается в дизайн/выбор подхода — зафиксируй решение в docs/ и продолжай, не устраивай архитектурный «битый триксель» 😅
