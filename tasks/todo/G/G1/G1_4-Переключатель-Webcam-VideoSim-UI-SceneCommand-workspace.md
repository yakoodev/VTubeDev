# G1_4 — Переключатель Webcam ↔ VideoSim (UI + SceneCommand + workspace)

**Status:** todo


## Цель

Сделать тот самый переключатель между настоящей вебкой и видео-заглушкой. Чтобы было как в мемах: 'настоящая камера' и 'камера из прошлого' 😄

## Контекст / ссылки

Зависит от базовой Debug UI панели (см. G1_0).


## Объём

**Включено:**

- InputSourceMode enum
- InputSourceManager
- SceneCommand SetInputSource
- UI toggle
- Persist to workspace


**Не включено:**

- Автосвитч по детектору лица




## Шаги реализации

1. Добавить InputSourceManager, который держит текущий IFrameSource и может переключать.
2. Добавить SceneCommand `SetInputSource(mode)`.
3. Добавить UI toggle (runtime панель) и отображение активного режима.
4. Сохранять выбор в workspace.json.


## Критерии приёмки

- [ ] Переключение работает без перезапуска.
- [ ] Через POST /cmd можно переключить режим.
- [ ] После перезапуска режим сохраняется.


## Как проверить

- В UI включить VideoSim — видно mp4.
- Переключить на Webcam — видно живую камеру.
- Перезапустить — выбранный режим сохранился.
- POST /cmd SetInputSource(VideoSim) — сработало.


## Артефакты

- InputSourceManager
- UI toggle
- SceneCommand + web demo payload
